<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"> <!-- Added background color -->

    <!-- Email EditText -->
    <EditText
        android:id="@+id/email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Email"
        android:inputType="textEmailAddress"

        android:layout_marginTop="16dp"
        android:textSize="16sp" /> <!-- Adjusted font size -->

    <!-- Password EditText -->
    <EditText
        android:id="@+id/password01"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Password"
        android:inputType="textPassword"
        android:layout_below="@id/email"
        android:layout_marginTop="16dp"
        android:textSize="16sp" /> <!-- Adjusted font size -->

    <!-- Sign Up Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/signup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/password01"
        android:layout_marginTop="32dp"
        android:text="Sign Up"
        android:textSize="18sp" /> <!-- Adjusted font size -->

    <!-- Login Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/signup"
        android:layout_marginTop="16dp"
        android:text="Log In"
        android:textSize="18sp" /> <!-- Adjusted font size -->

    <!-- Forgot Password TextView -->
    <TextView
        android:id="@+id/Forgotpassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/login"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="144dp"
        android:layout_marginTop="105dp"
        android:layout_marginEnd="153dp"
        android:layout_marginBottom="346dp"
        android:text="Forgot Password?"
        android:textSize="14sp" /> <!-- Adjusted font size -->

</RelativeLayout>
